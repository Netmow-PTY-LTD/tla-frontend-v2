'use client';

import React, { useState, useEffect, useCallback } from 'react';
import DistanceMap from '../_components/DistanceMap';

export default function DistanceSelector() {
  return (
    <div>
      <DistanceMap />
    </div>
  );
}
